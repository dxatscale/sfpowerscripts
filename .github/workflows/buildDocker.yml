name: Build and Publish  Docker Image to DockerHub


on:
  workflow_call:
   inputs:
    version:
      type: string
      default: 'alpha'
    environment:
        type: string
        required: true
   secrets:
      gh-token:
        required: true
jobs:

  build:
    name: 'Build docker image'
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v2
      with:
        fetch-depth: 0


    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Expose GitHub Runtime
      uses: crazy-max/ghaction-github-runtime@v1

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.gh-token }}


    - name: 'Get package version'
      run: |
        echo "PKG_VERSION=$(jq -r ".version" packages/sfpowerscripts-cli/package.json)" >> $GITHUB_ENV



    - name: Build and push
      id: docker_build
      uses: docker/build-push-action@v2
      with:
          file: dockerfiles/Dockerfile.test
          context: dockerfiles
          push: true
          tags: |
              ghcr.io/dxatscale/sfpowerscripts:${{ env. PKG_VERSION}}-${{ inputs.version }}
              ghcr.io/dxatscale/sfpowerscripts:${{ inputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: | 
                  SFPOWERCRIPTS_VERSION=${{ env. PKG_VERSION}}
